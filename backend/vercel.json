{
    "version": 2,
    "buildCommand": "npm install && npm run build && ls -R dist",
    "builds": [
        {
            "src": "backend/server.ts",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "backend/server.ts"
        }
    ]
}