{
    "version": 2,
    "builds": [
        {
            "src": "dist/server.ts",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "dist/server.ts"
        }
    ],
  "buildCommand": "npm install && npm run build"
}